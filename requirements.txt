# ─── Core Framework ───────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0

# ─── Market Data ──────────────────────────────────────────────
# ★ NEW (Fix 4) — yfinance >=1.5 includes the rewritten cookie/crumb
# auth layer that fixes the recurring HTTP 401 "Invalid Crumb" errors.
# The 0.2.x line is unmaintained; 1.x rewrote the session layer.
yfinance>=1.5

# ─── TradingAgents Engine ────────────────────────────────────
# ★ NOTE — installed from GitHub (pinned tag) by bootstrap.sh.
# Do NOT add a PyPI line here; the git install in bootstrap.sh
# takes precedence and avoids version conflicts.

# ─── Data / Numeric ──────────────────────────────────────────
pandas>=2.0
numpy>=1.24

# ─── Charting & Artifacts ────────────────────────────────────
matplotlib>=3.7
mplfinance>=0.12
kaleido>=0.2

# ─── AI / LLM Engine ─────────────────────────────────────────
langchain>=0.2
langchain-openai>=0.1
langchain-anthropic>=0.1
langchain-google-genai>=0.1
langgraph>=0.1
openai>=1.14

# ─── HTTP / Networking ───────────────────────────────────────
httpx>=0.27
requests>=2.31

# ─── Misc Utilities ──────────────────────────────────────────
python-dotenv>=1.0
fredapi>=0.5